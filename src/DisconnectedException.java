/**
 * Create a class to deal with the problem that is disconnected.
 *
 */
public class DisconnectedException extends Exception {
/**
 * Start the exception that deal with the disconnected problem.
 * @param message - exception message
 */
public DisconnectedException(String message) {
super(message);
}
}
